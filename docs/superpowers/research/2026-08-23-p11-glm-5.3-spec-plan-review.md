Let me work through this carefully. I'm reviewing a spec and plan for P11 "Subagent pool" in a macOS local-LLM app. I need to be adversarial and technically rigorous. Let me go through both documents systematically.

First, let me establish the settled context:
- Engine is a spawned child process (never linked)
- Laguna model family excluded from cross-session batch path unconditionally → pool is a queue over ONE serialized engine
- Win is context curve: 8×16k sequential prefills ~4.2x cheaper than one 131k prefill (upper bound)
- Session cost: ~6.1 GB GPU scratch (min(ctx,16384)*375156 bytes) + KV (49152*ctx + 75497472 bytes)
- Engine has session_new/rewind/save_payload/load_snapshot/common_prefix/eval but ds4_agent.c is single-worker
- P9 added --host-tools (tool_request/tool_result bidirectional wire)
- P10 added HandoffPacket + WorktreeDispatch.verdict + DispatchOutcome (candidate ref / receipt) + writableFiles revision check
- Session .kv file stores full rendered conversation as UTF-8 behind fixed 48-byte header (readable without inference)

Now let me review the SPEC for:
- internal contradictions
- decisions that don't follow from gardenable facts
- requirements that could be read two ways
- anything that would force a plan rework

And the PLAN for:
- spec coverage gaps
- tasks mis-sized
- the C-patch task (Task 8) — is the refactor sound, is "N==1 byte-identical / recapture gate" the right safety rail
- hand-authored-fake deviation — acceptable given "fakes are never hand-authored" rule?
- type/interface consistency across tasks
- places a no-context engineer would get stuck

Let me start with the SPEC.

**SPEC Analysis:**

D1 — One engine, N workers, serialized. "multiplexing a `worker` id onto the wire so the app knows which session an event belongs to." OK. But wait — there's a question about how the app addresses a specific worker. The spec says events flow out with worker ids, but how does the host send a prompt to a specific worker? D4 says "the app runs the worker's turn as a separate session" — but the mechanism for addressing worker N on stdin isn't specified in the spec. The plan Task 8 Step 5 invents it: `{"t":"prompt","worker":<id>,"s":"..."}`. That's a spec gap — the wire contract is supposed to be "fixed first" (D2 part 1) but the inbound direction (host→engine worker addressing) is not in the spec at all. D2 says "Fix the `worker`-id wire contract" — but the spec only describes the outbound direction. The plan invents the inbound protocol in Task 8 (the C patch task), which is backwards from wire-contract-first: the fake in Task 6 doesn't need to test inbound worker addressing because the fake just replays. Actually wait — does the fake need to respond to prompts addressed to workers? The fake replays a capture verbatim; the integration test writes a prompt "go" and reads events. So the inbound worker-addressing contract is never tested against the fake, and never specified in the spec. That's a real gap: the "wire contract" that D2 says is fixed in part (1) is only half a contract (outbound events). The typed contract in Swift (PoolWireParser) only parses outbound. The inbound prompt routing (`{"t":"prompt","worker":N,"s":...}`) appears first in Task 8 Step 5 of the plan, as an "or" choice ("Route stdin prompts by an optional leading worker selector — **or**, matching D4, keep the orchestrator as worker 0 and let the app address a worker turn with the existing prompt mechanism plus a `worker` field on the prompt line. Pick the minimal form..."). That's a decision deferred to the implementer — a no-context engineer would trip. And the fake (Task 6) doesn't exercise it, so the Swift side has no typed representation of "send prompt to worker N" — PoolEngine in Task 7 only builds argv; there's no `PoolEngine.send(prompt:to:)`. Task 9 says "run each via the existing `runDispatchedTurn` (P10)" — but P10's runDispatchedTurn presumably drives a single-session engine with plain prompts. How does the app send the worker's packet to worker N in the pooled engine? There's a missing piece: the app-side API to address a worker. This is a blocker-level gap: Task 9 depends on an inbound mechanism that no task defines on the Swift side.

Wait, let me re-read Task 9 Step 4: "On the orchestrator's turn-end `ready`, drain any queued workers (`PoolScheduler.nextWorker`), run each via the existing `runDispatchedTurn` (P10)". Hmm — P10's dispatched turn ran in "a disposable worktree by a fresh single-session engine". So maybe the intent is that the worker turn is run... in the same pooled engine? Or a separate engine? The spec D1 says one engine, N sessions. The data flow (step 2) says "The scheduler runs it (the only generating session): the worker's turn produces its own `worker`-id-scoped events". So the worker turn runs in the pooled engine as worker N. But Task 9 says "run each via the existing `runDispatchedTurn` (P10)" — P10's runDispatchedTurn spawns a fresh engine. If they reuse it verbatim, they'd spawn a second engine process, contradicting D1 (one model load). If they adapt it, the adaptation needs the inbound worker-addressing mechanism, which doesn't exist in the plan's Swift tasks. Either way Task 9 is underspecified and possibly contradictory. This is a blocker.

Also: the worktree. P10 ran the dispatched attempt in a disposable worktree. In the pool, workers share the parent's root (D8: "Short-lived, shares the parent's root (bootstrap prefix)"). Wait — "shares the parent's root" — is that the filesystem root/workspace or the session root/bootstrap prefix? It says "(bootstrap prefix)" so it means the KV prefix. But P10's isolation was a disposable worktree; does the pooled worker still get a worktree? The spec's data flow step 2 says "the P9 responder revision-checks mutations against `packet.writableFiles`" — revision check, but where does the worker's file mutation happen? If all workers share one workspace/process, two workers' mutations could interleave... but serialization means one at a time. But the orchestrator session and worker sessions share the process — the P9 host-tools responder is per-process? How does the host know which worker a tool_request belongs to? It does — worker id on the event. OK. But the worktree question: P10's DispatchOutcome involved a candidate ref (a git commit in a worktree). In the pool, does each worker get its own worktree? The spec doesn't say. D8 says workers are "short-lived, shares the parent's root (bootstrap prefix)". If workers run in the parent's working tree, then a worker's mutations hit the parent's files directly — that contradicts P10's disposable-worktree isolation and makes "candidate ref" production impossible without a worktree (you can't commit a candidate ref without a separate branch/worktree without disturbing the parent's index). Actually you could commit on a branch in the same repo... but the parent's index would be dirty. Hmm. The spec is silent on worktree-per-worker. The plan Task 9 says "run each via the existing `runDispatchedTurn` (P10)" which presumably includes worktree creation — but then the engine process for the worker... P10 spawned a fresh engine whose workspace was the worktree. In the pool, one engine process has one `--workspace` argv. All N sessions share the same workspace path! So a worker's tool mutations (file edits) happen in the orchestrator's workspace unless the host-side responder (P9 host-tools) executes them — wait, with --host-tools, tool execution is host-side: the engine emits tool_request, the host executes, returns tool_result. So file mutations are executed by the HOST, not the engine. So the host can execute worker N's edits in worktree N while the engine process's --workspace is just where the engine reads? Hmm, but read tools (read/list) — are those host-executed too under --host-tools? P9 says the wire is bidirectional with engine blocking on result; "A `dispatch` is just another host-executed tool" (D3). So presumably read tools are also host-executed under --host-tools. Then the engine's --workspace matters less. But this is all inference — the spec doesn't address how per-worker worktree isolation maps onto one engine process with one workspace. The plan doesn't either. Task 9's "reusing `WorktreeDispatcher.dispatch` with the worker session" (in Interfaces: "run the worker (reusing `WorktreeDispatcher.dispatch` with the worker session)") — so they intend to reuse WorktreeDispatcher which creates the worktree and runs the turn. But WorktreeDispatcher.dispatch presumably spawns its own engine process (P10: "run in a disposable worktree by a fresh single-session engine"). Reusing it with "the worker session" means what — pointing it at the pooled engine? That's a significant adaptation, not a reuse. Undersized task / missing design. I'll flag this as a blocker: the worktree/worker-turn execution model is ambiguous between "worker turn runs in the pooled engine process" (spec data flow) and "reuse P10 WorktreeDispatcher which spawns a fresh engine" (plan Task 9), and these contradict D1's one-engine claim if taken literally.

Actually wait. Let me re-read the spec data flow: "2. App enqueues worker N. The scheduler runs it (the only generating session): the worker's turn produces its own `worker`-id-scoped events; the P9 responder revision-checks mutations against `packet.writableFiles`; the pure `WorktreeDispatch.verdict` returns a candidate ref or a receipt (P10 unchanged)." So the worker turn runs in the pooled engine (worker-id-scoped events). The verdict is P10's pure verdict. So the worker turn is in the pool. Then Task 9's "run each via the existing `runDispatchedTurn` (P10)" must mean adapting it to drive the pooled engine's worker N. But then: how does the app feed the worker's prompt (the packet's prepared taskText) to worker N? Via the stdin worker-addressing from Task 8 Step 5 — which is only defined in the C task, chosen among alternatives ("Pick the minimal form"), and has no Swift-side API or test. And how does the host execute worker N's tool calls in a per-worker worktree when the responder is a single host-side responder? The responder gets tool_request with worker id; it must route execution to worktree N. Task 9 Step 1 adds `dispatch` to the responder but says nothing about routing worker tool calls to per-worker worktrees. Hmm, but maybe P10's WorktreeDispatch already handles this per-dispatch. The integration is genuinely murky. This is the biggest plan gap: Task 9 is mis-sized (it's the heart of the phase — orchestrator loop, worker turn execution in the pooled engine, worktree wiring, receipt injection — and it's sketched in 6 thin steps with no failing-test-first detail for the controller, unlike Tasks 1–5).

Also note Task 9 has no Step "write failing tests" for the controller wiring except the responder tests. The controller wiring (AgentController) is App-tier — is it testable? The plan's testing tiers: fast (Kit), integration (AppKit), live. The App (SwiftStar) layer has no test tier mentioned. Task 9 Step 5 runs `just test` + `just integration` — but what test covers the orchestrator loop? Only the responder tests. The receipt-injection-on-next-turn behavior (D4) — the spec's integration tier says "the receipt correlation (worker N's outcome lands on orchestrator turn N+1)" is integration-tier. Which task implements that integration test? Task 6's PoolEngineTests only covers fixture replay routing. Task 9 doesn't add an integration test for enqueue → run → receipt-inject. Spec's Testing section: "Integration tier: the fake engine's `worker`-id wire against the Swift pool (enqueue → run → receipt-inject)". No plan task delivers "enqueue → run → receipt-inject" against the fake. Task 6 tests parsing/routing only. Task 9 tests only the responder. Coverage gap — blocker or should-fix. I'd say should-fix-to-blocker: the spec's integration tier explicitly names it; the plan's self-review claims D4→Tasks 1,9 but Task 9 contains no such test. I'll call it a blocker for spec coverage since the spec's Testing section is a requirement.

**D4 analysis:** "The receipt ... is never delivered mid-turn: the app runs the worker's turn as a separate session, and on completion injects the receipt into the orchestrator's *next* turn as a prompt." Two-way reading: "next turn" — does the orchestrator's turn end at EOS after dispatch (data flow step 1 says "the turn ends at EOS"), then the app runs workers, then injects the receipt as the next prompt — meaning the orchestrator's next turn is initiated by the receipt injection? Or does the user's next message trigger a turn into which the receipt is prepended? The data flow says "App injects the receipt into the orchestrator's next turn as a prompt" — so the app drives the orchestrator with the receipt as prompt. But then who decides when the orchestrator stops? If the orchestrator dispatched 3 workers in one turn, the app receives 3 receipts; does it inject 3 separate prompts (3 orchestrator turns) or one combined? The plan's Task 9 Step 4: "enqueue `receipt.injectionPrompt()` as the orchestrator's next prompt" — singular per worker. If two workers complete, two injections → two turns? Or batch? Ambiguity. Also: what if the orchestrator's dispatch turn ends and the model expects an answer to its tool call — wait, D4 says the host answers ok:true immediately ("dispatched as worker N"), so the tool_result is immediate; the turn continues and ends at EOS. Fine. Then the app runs workers and injects receipts. But there's a subtle issue: the orchestrator might dispatch a worker and then keep generating (more dispatches, or text). The app must wait for EOS, then run all queued workers, then inject. PoolScheduler has pendingDelivery as a set — injection order? Smallest worker id? Unspecified but minor.

Another D4 ambiguity: "injects the receipt into the orchestrator's *next* turn as a prompt" — could be read as (a) the receipt text becomes the user prompt for a new turn, or (b) the receipt is appended to the context of whatever turn comes next (user-driven). These have different implementations. The plan picks (a)-ish. The spec should pin it. Should-fix.

**D6 analysis:** "Rolling digest ... maintained incrementally by the host as events arrive, with no model and no inference. Backed by the `.kv` rendered text (readable inference-free)." Contradiction-ish: the digest is maintained from wire events (the reducer consumes PoolWireEvent), so what is the .kv backing for? The .kv is the raw conversation on disk; the digest is the reduced form. "Backed by" is vague — is the digest reconstructed from .kv on restart (crash recovery)? Or is .kv just the fallback/audit? The plan's RollingDigest only folds wire events + host verdicts + receipts; it never reads .kv. Task 7 implements readKVText but nothing consumes it. So the .kv reader is dead code in the plan — no consumer. Spec coverage gap: D6 says the digest is "backed by" the .kv text; the plan implements a reader and never uses it. Either the spec means something (reconstruction on relaunch — which matters: the pool ledger must survive app restart per D9 "reconstructed host-side from the rolling digest" — if the app restarts, the digest is gone unless reconstructed from .kv) or it doesn't. If it does mean reconstruction, no task implements it. Flag as should-fix: define what "backed by" means; either wire readKVText into a reconstruction path or cut the claim.

Also D6: "The extraction prefill is over this small digest, never the raw conversation." What extraction prefill? This is the only mention of an "extraction" step in the spec. D7 is the adaptation trip. Is "extraction prefill" the D7 trip's prefill (over the digest)? Probably. But it's a new term introduced once, unexplained — could be read as a third model trip. Nit/should-fix: terminology.

**D7:** "Runs reasoning-light (think off): cheaper on the decode side and keeps think tokens out of the folded-back context, but it does not cut prefill — that is D6's job." Wait — the adaptation trip's prefill is over the digest (small). It says prefill cutting is D6's job. Fine. But: which model runs the adaptation? The orchestrator model (Laguna) in a separate small session? The spec doesn't say which session/model performs D7's trip, or when it runs (before each dispatch? at packet-making time). The plan's Task 4 makes adaptationPrompt pure and says "The model trip that produces `adaptation` (D7) is impure and lives in the app" — but NO task implements the adaptation model trip! Task 9 doesn't run a no-think session to produce the adaptation. ContextAssembly.assemble takes `adaptation: String` as a parameter from nowhere. So D7 — a named decision with a whole task's worth of machinery (a no-think model trip over the digest) — has zero implementation coverage. The self-review claims "D7→Task 4" but Task 4 only builds the prompt text, never runs the trip, and nothing calls adaptationPrompt in production code. Blocker: spec coverage gap. D7 is a Decision; decisions are the authority. If the plan intends D7's trip to be deferred, the spec/plan must say so (it doesn't — out-of-scope lists don't include it).

Hmm wait, let me double-check the out-of-scope list: "The model-driven RLM sub-query tier...; full A-routing...; specialized tool subagents; multi-project residency; the `recall` tool and session browser; merging any candidate ref". No mention of deferring D7's trip. And D5's pipeline is "deterministic-load → rolling digest → objective-dependent adaptation → packet" — the packet-maker includes the adaptation. The plan's packet path: Task 9 enqueues... what packet? Task 9 Step 1: the dispatch tool's params carry taskText and writableFiles. So the orchestrator MODEL authors the packet's taskText via the dispatch tool call?! But D5 says the packet's prepared context is assembled by the host (load + digest + adaptation), "not P10's bare taskText". Contradiction between D5 and the plan's Task 9: in Task 9 the dispatch tool params carry taskText (model-authored), and there's no host-side assembly step in the dispatch path. The fixture in Task 6 also shows dispatch params with taskText. So the plan implements P10's bare taskText dispatch, not D5's context assembly. ContextAssembly.assemble exists but is never invoked in the dispatch flow. This is a major spec-coverage blocker: D5 (a titled Decision) is not wired into the actual dispatch path; the plan builds the pure mapping (Task 4) and drops it. Unless the intent is that the host intercepts dispatch and re-assembles taskText = assemble(objective: <model's taskText>, digest:..., loaded:..., adaptation:...) — but the adaptation requires the D7 trip which isn't implemented. So the whole D5/D7 pipeline is dead code in the plan. Blocker.

Let me also check: does anything call `PoolWireParser` in production (AppKit drain)? Task 7 says "PoolEngine.drain(...) route events by worker" in Interfaces/Produces, but the implementation shown in Task 7 Step 3 contains only argv, readKVText, listFiles — no drain. So `drain` is named in the interface list but not implemented in the task body. A no-context engineer hits the Interfaces section promising drain and the implementation not having it. Should-fix (or fold into the Task 9 blocker about missing inbound API). Also `PoolEngine.spawn(settings:workers:) -> Process` is in Interfaces but implementation shows `argv(...)` only, no spawn. Same issue. The task's "Produces" list doesn't match its steps. Should-fix.

**D8:** "a worker's ctx_size equals its budget, and exceeding it is a receipt, never a compaction." How does a worker's ctx_size get set per-worker in the pooled engine? The engine argv has one `-c` value (Task 6 fixture uses `-c 8192`). Does --subagent-pool N create N sessions each with the same ctx_size from -c? Then per-worker budgets equal the global ctx. But D8 wants budget = ctx_size per worker, and D-facts say small-ctx workers are a lever (~1.5GB at 4k). If all sessions share -c, the lever is gone and memory is N × (scratch + KV) at full ctx — with the 6.1GB scratch per session at ctx≥16k, N=8 workers = ~49GB scratch + KV... on what GPU? This is a real design hole: the spec's own memory facts argue for per-worker ctx, but neither spec nor plan defines how per-worker ctx_size is communicated (argv? per-worker flag? session_new param at dispatch time?). Task 8 doesn't touch it. Also: sessions are created when? At engine start (N sessions upfront = N × 6.1GB immediately) or lazily on first dispatch? Spec doesn't say; plan doesn't say; Task 8 Step 4 says "Each workers[i] owns its own ds4_session (created via ds4_session_new from the shared engine)" — created at startup, presumably. With N=2 at 16k ctx that's ~12.2GB scratch + KV ~ (49152*16384+75MB)*2 ≈ 1.6GB+75MB each... wait KV = 49152*ctx + 75,497,472 bytes = 49152*16384 ≈ 805MB + 75MB ≈ 880MB per session. So ~7GB per session at 16k. N=8 → ~56GB. On Apple Silicon unified memory (say 64-96GB max, with OS overhead), 8 workers at 16k is borderline-impossible; even the gate's "8 × 16k" runs sequentially — do all 8 sessions need to exist simultaneously? If the win requires 8 shallow sessions alive at once (to avoid re-prefill), memory blows; if sessions are created/destroyed per worker turn, then... the shared sysprompt.kv prefix is reloaded per session (cheap-ish), but the orchestrator's session stays alive. Actually for the gate: one deep 131k baseline vs 8 shallow 16k runs — the 8 shallow runs could be sequential in ONE session (rewind between)? No — the 4.2x is 8 separate 16k prefills; they could reuse one session with rewind. Hmm, but the pool's real usage: orchestrator session (grows) + workers. The spec's memory lever "small-ctx workers and snapshot/rewind of idle tiers" implies workers at small ctx. But D8 says budget=ctx_size and the packet's turnBudget (HandoffPacket has turnBudget: 1000 in tests — tokens? turns?). HandoffPacket.turnBudget: 1000 — if that's tokens, ctx_size=1000?? The scheduler test creates packets with turnBudget: 1000. If ctx_size = budget = 1000 tokens, that's a tiny ctx — min(ctx,16384)*375156 → 1000*375156 ≈ 375MB scratch. OK plausible. But how does turnBudget become the session's ctx_size in the pooled engine? No mechanism. The engine gets -c once. Blocker/should-fix: per-worker ctx sizing is unspecified end-to-end (spec D8 asserts it; no plan task implements it; Task 8's C patch doesn't add a per-session ctx or a way to set it). I'd rate should-fix to blocker. Given D8 is a decision ("a worker's ctx_size equals its budget"), and nothing implements it, and the memory math makes the default (shared -c) potentially infeasible for the gate's own 8×16k configuration... Actually for the gate, workers at 16k ctx: 8 sessions × ~7GB = 56GB — the gate might not even run on the dev machine. Or the gate could run workers sequentially reusing fewer sessions. The plan Task 10 Step 1: "runs one deep-context baseline (131k) and the pooled shallow-context runs (8 × 16k, sequential)". Sequential 8×16k — could be one session rewound 8 times, or 8 sessions. If the point is to measure the pool, it's 8 sessions in the pool... at 56GB. Or maybe the pool with N=2? The gate's configuration is unspecified w.r.t. memory feasibility. Should-fix: the plan must state session lifetime (lazy vs eager) and per-worker ctx, and check the gate's memory envelope against the target machine.

Hmm, actually wait: is scratch per-session or per-engine? "a session costs ~6.1 GB GPU scratch (min(ctx,16384)*375156 bytes)". 16384*375156 = 6.146e9 ≈ 6.1GB. Yes per session. So N sessions = N×6.1GB scratch. This is a big deal for N. The spec never states max N or the memory feasibility of N. D1 says "N workers" unbounded. Should-fix: spec should state the N bound implied by memory (or lazy session creation + eviction... but eviction is explicitly "zero in v1 is itself a finding" — D11 says instrumentation answers "does the pool need idle-session eviction at all", implying v1 has no eviction, so all sessions live forever → memory = N × per-session cost from startup. With eager creation at startup (Task 8 Step 4), even N=2 doubles GPU memory before any dispatch. And the orchestrator's own session at large ctx (131k? no — orchestrator ctx is whatever -c is)... The deep baseline is 131k ctx = 6.1GB scratch + KV 49152*131072+75MB ≈ 6.4GB+75MB ≈ 6.5GB → ~12.6GB for one deep session. OK.

So the memory story: pool of N=8 at 16k ≈ 8×(6.1+0.88) ≈ 56GB. The gate says "8 × 16k" — if that's the pool config, it needs ~56GB free. Feasible on a 64GB machine barely, not on 32GB. The plan doesn't check. Should-fix.

**D9:** "Compaction on the orchestrator resets warm-prefix routing (exact-prefix-only KV reuse): the summary, not the previously-read files, becomes the new prefix." Fine. "its pool ledger ... is reconstructed host-side from the rolling digest (D6), never trusted to the model's summary." OK — but reconstruction happens when? At compaction time the host re-injects the ledger? Or the digest IS the ledger (already host-side)? "Reconstructed" implies rebuild-from-digest into... the orchestrator's context? The plan: nothing implements orchestrator compaction integration (the digest summary is claimed as the ledger in self-review: "the compaction ledger reconstruction (D9) is the digest's `summary()` (Task 3), consumed host-side"). Consumed by what? Nothing consumes summary() except ContextAssembly.adaptationPrompt/assemble. No compaction path exists in the plan. Is orchestrator compaction in scope for P11? The spec D9 says "The orchestrator alone compacts" — is that describing existing behavior (pre-P11 compaction exists?) or new? The gardenable facts mention "the pre-compaction prefix survives on disk until evicted" — so compaction exists in the engine already. D9's rule is about what the host does at compaction: reconstruct the ledger from the digest. If orchestrator compaction is triggered by the engine, the host's job is to include the ledger in the post-compaction context. No task does this. Coverage gap, should-fix (arguably the compaction-ledger wiring is small but it's claimed covered when it isn't).

**D10:** "The wire contract reserves the routing types now" — what routing types? Parallelism proofs, thinking-requirement test, executor choice are behaviors, not wire types. What would a "reserved type" look like? Maybe DispatchReceipt gains fields for executor choice? The plan reserves nothing — DispatchReceipt has worker/ref/reason/summary. No reserved fields/types anywhere. So D10's "reserve the routing types now" is unimplemented. What types did they mean? Unspecified in the spec itself — the spec decision is unactionable as written ("reserves the routing types now" without naming them). A no-context engineer cannot implement D10. Should-fix: name the reserved types (e.g., an `executor` field on HandoffPacket or DispatchReceipt, a `parallelism` proof slot) or drop the claim. Also the use-it-or-lose-it governance (D11 tail): "if A-routing has not landed within one phase after P11, divergence #11 is flag-gated off or reverted" — governance, not code; fine, but the plan's Task 11 (close phase) doesn't record this commitment anywhere (ROADMAP note?). Nit.

**D11:** The gate. "overhead ratio — realized win / analytic 4.2x ceiling on canonical packets". Realized win = deepSeconds/poolSeconds (plan defines). OK. "sensitivity envelope — deterministic perturbations (taskText bloat ×1.5/×2, failure injection at fixed rates under a fixed retry policy, packet-count sweeps), yielding win-as-a-function-of-packet-quality and the threshold where the win stops justifying the fork". Hmm — "the threshold where the win stops justifying the fork" — a judgment made how? By whom? The gate produces data; the threshold is a human read. Fine. But note: failure injection "under a fixed retry policy" — what retry policy? P10 had verdict outcomes; is there a retry policy in existence? "fixed retry policy" is unspecified — the plan's PacketPerturbation.failureInjection exists as a case but Task 10 doesn't define the retry policy or the injection rates. Also packetCountSweep — sweep over what range? Unspecified. Task 10 Step 1 says "builds the packet set (canonical + the deterministic perturbations)" — the deterministic perturbation generators (bloat ×1.5/×2 — bloat with WHAT? random text? deterministic filler?) are not specified or implemented anywhere in Kit. EnvelopeMath takes winByPerturbation as an input dictionary — the perturbation construction lives... in the gate script (Task 10), untested. Should-fix: perturbation constructors should be pure Kit functions (deterministic, testable) rather than buried in a shell script; spec's component list says Kit contains "the envelope computation" — constructing perturbations is arguably part of it.

Also D11(c): "tokens actually evaluated vs nominal (where snapshot/restore re-eval bugs hide)" — how are tokens-actually-evaluated measured? Engine status events report prefill_done/prefill_total? The fixture shows prefill fields on status. OK, plausibly from status events. peak resident memory — of the engine process (footprint via sysctl?) — the gate script must measure it; unspecified mechanism but that's live-tier detail. Nit.

**Contradiction check — D2 vs the fake rule:** The repo rule is "fakes are never hand-authored" (evidence floor: fake regenerated from a committed capture). D2 knowingly deviates: hand-authored against the typed contract until the recapture. Is that acceptable? The point of never-hand-authored is that a fake derived from a capture can't encode a wrong belief about the wire — the capture is ground truth. A hand-authored fake against the typed contract means the Swift types and the fake share one author's assumptions; if the C patch's actual wire differs (field name `worker` vs `wid`, placement, hello caps changes), the fake will happily confirm the wrong contract and the recapture will break it — which is exactly what the recapture gate catches. So the deviation is acceptable ONLY because (a) it's explicitly time-boxed and retired by Task 8's recapture, and (b) the recapture is byte-compared. But there's a subtle trap: Task 6's integration test asserts routing against the hand-authored fixture; when Task 8 replaces pool.ndjson with a real capture, the test's assumptions (e.g., dispatch tool_request on worker 0, worker 1 events present, exact hello caps) may not hold — e.g., the real engine might emit `worker` on hello only when N>1, or the real pool capture's hello might include new caps. The plan says regenerate the fake from both captures, but doesn't say the Task 6 test must be re-verified/adapted against the real capture — it should state that Task 6's test assertions are re-run and updated where the real wire differs, and that ANY difference is a contract bug found, not a test to relax silently. Also: the hand-authored fixture includes `"worker":0` on the hello line — but Task 8 Step 3 says worker field is emitted "only when cfg->num_workers > 1"... on EVERY event? If N>1, is worker on hello too? The fixture says yes. But also — hmm, Task 8 says absent when N==1. In the pool capture (N=2), hello carries worker:0. Consistent. But PoolWireParserTests.absentWorkerDefaultsToOrchestrator feeds a hello WITHOUT worker then a text without worker — fine.

One more D2 wrinkle: "the fake is regenerated from a golden capture once part (2) exists" — but the fake generator (FakeAgentSource.generate) takes capture + engineArgv. The fake validates argv strictly. After Task 8, the pool capture's argv includes --subagent-pool 2 — fine. But the SINGLE-session golden recapture: Task 8 Step 6 recaptures golden.ndjson "with the default argv (no --subagent-pool, so N == 1)" and confirms byte-identical modulo ts. Good — that's the right safety rail for the C patch. Is "byte-identical modulo ts" the right gate? The events include `ts` (timestamps) and possibly other volatile fields (power? The status events carry "power":100 — is power real (varies)? In a capture, power might vary run to run!). If status events include power readings that vary, byte-identity modulo ts will fail spuriously. The plan says "modulo `ts`" only. If power/prefill_tps/gen_tps vary between runs (they will on real hardware — tps especially!), the recapture can never be byte-identical. Hmm — but the standing rule presumably already handles this (P9/P10 recaptures existed with status events). Maybe the existing normalization strips more than ts. The plan asserts "byte-identical modulo `ts`" — if the repo's existing recapture tooling normalizes volatile fields, fine; but as written, a no-context engineer will hit spurious diffs on tps/power and not know whether to stop (the risk note says "Stop and report if the recapture at (a) differs beyond ts — that is a semantic collision"). tps differences would trigger false stops. Should-fix: define the normalization set precisely (ts, tps, power, prefill timing) or point to the existing recapture normalizer. This matters because the stop rule's false-positive rate determines whether the gate is usable.

Also on Task 8: is "N==1 byte-identical" the right safety rail at all? It's necessary but weak: it proves the patch doesn't change single-worker output, but the pool behavior (N>1) — session interleave, worker routing, one-at-a-time — is only covered by two C tests (emit worker id, prompt routing) and a small pool capture. The serialization invariant ("exactly one generating at a time") has NO test — neither C-side nor capture-side. How would you test it? A capture of a 2-worker run where turns interleave... the capture would show worker 1 events not overlapping worker 2 generation — hard to prove from a replay. A C unit test asserting the pool mutex / a state invariant (e.g., an assert in the eval path that no other worker is mid-turn) would do it. The plan's Task 8 Step 4 says "add a pool mutex so only one worker's turn runs at a time" — untestable as described? Could add a debug counter asserted at turn boundaries. Should-fix: add an explicit serialization assertion/test, since the serialized invariant is D1's core claim and the whole memory/throughput model depends on it.

Bigger Task 8 soundness question: the refactor description. Current: single `agent_completion_worker` global (~414), per-worker thread/mutex/cond. Proposed: array of workers, each with own session, keep per-worker thread/mutex/cond, add pool mutex. Concerns:
1. If each worker keeps its own thread and they all block on the GPU... the engine's eval is serialized by... what? The gardenable fact says the family is excluded from cross-session batching and the fallback is a sequential eval loop — that's about the ENGINE's internal batch path across sessions in one eval call, not about concurrent threads calling eval. Two worker threads calling ds4_session_eval concurrently on sessions sharing one engine — is that safe? The spec assumes "the serialization is already enforced by the single GPU" (Task 8 Step 4: "the serialization is already enforced by the single GPU — add a pool mutex so only one worker's turn runs at a time"). "Already enforced by the single GPU" is hand-waving: a single GPU doesn't serialize host threads; locks do. If ds4_session_eval isn't thread-safe across sessions on one engine context, concurrent calls race on the shared compute buffers (the 6.1GB scratch is per-session, but the model weights/scratch may be shared engine-side). The plan adds a pool mutex — good — but then says "only one worker's turn runs at a time", which makes the per-worker threads pointless (they'd all serialize on the pool mutex anyway). Simpler and sounder: ONE worker thread (the existing completion worker) multiplexing N sessions, no per-worker threads at all. Keeping N threads + a pool mutex is more code, more deadlock surface (per-worker mutex/cond + pool mutex ordering), for zero concurrency win — the spec itself says the win is NOT concurrency. So the described refactor (array + threads + pool mutex) is questionable; the minimal-risk refactor is: keep the single worker thread, add a session table, route by worker id. This also keeps N==1 closer to byte-identical (same thread structure). I should flag this as a should-fix redesign recommendation: drop per-worker threads in v1; single-threaded turn loop over sessions. It directly reduces the "largest mechanical change" risk the plan itself flags.

2. Stdin routing: Task 8 Step 5 offers two options and says "Pick the minimal form" — a decision delegated to the executor, in the wire contract, AFTER the fake was authored (D2 contract-first violated: the inbound contract isn't fixed by the fake/typed-contract phase because the fake never exercises inbound worker addressing — the fake replays regardless of prompt content? Actually FakeAgentHarness.writePrompt(fake, "go") writes a bare prompt; the fake presumably echoes/replays regardless). So the inbound half of the contract (how the app addresses worker N) is unspecified in the spec, untested in part (1), and decided ad hoc in part (2). That inverts D2. Blocker-ish: the spec must fix the inbound contract (D1 or D4 should specify it), and Task 1 or 6 should type it (e.g., a `PoolPrompt` encoder in Kit with tests), so the C patch implements a pre-fixed contract.

3. `atoi(need_arg(...))` — fine, matches house style presumably.

4. "worker field absent when N==1" — but what about N>1 hello: does hello carry worker:0? The fixture says yes; Task 8 Step 3 says append to every event object when num_workers>1 — consistent. OK.

5. Task 8 Step 1 anchor line numbers (~75–207, ~755–870, ~420–520, ~4852, ~414, ~55373–55414) — ds4_agent.c is huge (ds4.c:64276 referenced in spec, so files are tens of thousands of lines). Fine.

6. The gate "N==1 must be byte-identical" as THE safety rail: right call, but insufficient for the reasons above (no serialization test, no N>1 behavioral test beyond two emit tests). Also missing: what happens to --host-tools interactions in pool mode? tool_request now carries worker id; the host's tool_result must route back to the RIGHT session. Task 8 Step 5 routes stdin prompts by worker — but tool_result lines! Under --host-tools, the host sends tool_result on stdin. With N sessions, a tool_result must be addressed to the requesting worker. The plan's stdin routing covers `prompt` lines only ("a stdin line {\"t\":\"prompt\",\"worker\":<id>,\"s\":\"...\"} addresses worker <id>"). tool_result routing is unaddressed — blocker in the C patch design. The engine blocks on the result (P9); with two workers you must say which worker's tool call is being answered. Unless tool_results are answered strictly in-order and the engine correlates by pending-call queue per worker... but with one generating at a time, only one worker can have an outstanding tool_request at a time! Since exactly one session generates at a time, at most one tool_request is pending → tool_result can be unambiguous. Hmm — is that true? A worker's turn ends at a tool_request (engine blocks); no other worker runs until... who unblocks? The app must answer the tool_result before the turn completes; the scheduler's "one generating at a time" means the engine is stuck on worker N's tool until answered. So tool_result needs no worker tag IF the engine enforces one-outstanding-tool-request globally. But that's an invariant the C patch must establish and test; the plan doesn't mention tool_result routing at all. If the implementation instead allows the orchestrator to run while a worker blocks on a tool (it shouldn't — serialized), ambiguity arises. Flag: should-fix/blocker — specify tool_result addressing (or the one-outstanding invariant) in Task 8 and test it (a C test: two workers, tool_request from worker 1, tool_result untagged routes to worker 1).

Also: does the orchestrator session in the pool use --host-tools dispatch (D3) — the dispatch tool_request comes from worker 0; the host answers ok:true. Fine, same mechanism.

**Now the fake deviation question directly:** "is that acceptable given the repo's 'fakes are never hand-authored' rule?" My answer: conditionally yes — it's acceptable because (1) it's explicitly declared a stand-in with a retirement path (Task 8 recapture), (2) the contract it encodes is typed and tested in Kit, so the fake is derived from the typed contract rather than freehand, and (3) the recapture gate will surface any divergence as a hard stop. BUT the plan weakens the safety net in two ways: (a) the hand-authored fixture bakes in behavioral assumptions beyond the wire format (dispatch tool_request from worker 0, worker 1 status/ready sequence) that the real capture may not match, and Task 8 doesn't require re-validating Task 6's test semantics against the real capture — only "regenerate the fake from both"; (b) between Task 6 and Task 8, the Swift pool (Task 9 comes AFTER Task 8 — wait, order: Task 6 fake, Task 7 PoolEngine, Task 8 C patch, Task 9 app wiring). So the app wiring (Task 9) is built AFTER the real engine exists — good, the fake was only needed for Tasks 6–7. Actually then why is the fake needed at all for part (1)? D2 part (1): "Fix the worker-id wire contract, the fake engine, and the Swift pool against the fake" — the Swift pool = PoolEngine (Task 7). Task 7's tests: kv reader (no fake needed) — the argv/spawn isn't even tested with the fake in Task 7 (Task 6 tested argv against the fake). OK so the fake's window is Tasks 6–7 only; acceptable. But one more twist: Task 6's fake validates argv strictly including "--subagent-pool 2" — the fake's argv validator must ACCEPT a flag that doesn't exist in any real engine yet; after Task 8, fine. Between 6 and 8, the fake accepts it — that's the point. OK.

But here's a subtle one: FakeAgentSource.generate(capture:engineArgv:hostTools:) — the fake is GENERATED from the capture. The hand-authored artifact is the capture (pool.ndjson), not the fake per se. The rule says fakes are never hand-authored; a hand-authored capture generating the fake is the same deviation one level down. Fine, same analysis. The plan's global constraints state it as "the fake for part (1) is hand-authored against the typed contract" — wording nit: it's the capture that's hand-authored. Nit.

**Type/interface consistency checks across tasks:**

- Task 2's PoolState in the Interfaces section: `PoolState { pending, running, completed, nextId }` — but the implementation adds `pendingDelivery` and the test uses `s.pendingDelivery`. Interfaces section omits it. Minor inconsistency (nit) — an executor writing from the Interfaces block first would miss it, but tests catch it.

- Task 2: `PoolCommand.enqueue(packet:)` assigns WorkerId from nextId — but Task 9's flow: the responder answers "dispatched as worker N" — N must be the id the scheduler assigned. The responder (pure, Task 9 Step 1/3) returns ok:true "dispatched as worker N" — but the responder doesn't know N; the scheduler assigns it at enqueue. So the responder's message can't include N unless the controller enqueues FIRST and then formats the tool_result with the assigned id. Task 9 Step 1 says the dispatch tool "returns ok:true immediately (the host enqueues the worker and answers 'dispatched as worker N')" — sequencing: enqueue → get id → answer. Feasible but the responder's pure `respond` signature must take the assigned id (or the controller formats the result). Underspecified; a no-context engineer will puzzle over where N comes from. Should-fix/nit.

- Task 3 RollingDigest: `receipts: [WorkerId: String]` holds refusal reasons; `refs: [WorkerId: String]`. DispatchReceipt has both ref and reason and summary — record() puts ref→refs, reason→receipts, drops summary. But the ledger per D6/D9 wants "spawned worker → ref/receipt" — fine. But summary (the worker's one-liner) is dropped from the digest — the digest then can't reconstruct what the worker DID for the orchestrator's compaction ledger beyond "ref1". D5's assembled context includes digest — losing summaries makes the assembled context thinner than the spec's intent ("keep the host-authoritative ledger (files touched, refs, receipts, exit statuses)") — receipts here means ref-or-refusal; summary is arguably noise. OK, defensible. Nit at most.

- Task 3 test `stripsToolNoiseKeepsMutations` — the comment says the reducer records the call name only; the expectation `d.toolCalls == ["edit"]`. But wait — the digest is supposed to STRIP tool noise; keeping every tool call NAME is... the spec says "strip tool noise, keep the host-authoritative ledger (files touched, refs, receipts, exit statuses)". Tool call names are not in the keep-list! The reducer keeps toolCalls (names) — contradicts D6's keep-list. Either the spec's list is illustrative or the reducer over-keeps. Also toolCalls is per-orchestrator-and-workers mixed (events from all workers fold into one digest? apply(d, event:) takes PoolWireEvent — the digest doesn't partition by worker for tool calls; filesTouched accumulates across workers without attribution. For the ledger "spawned worker → ref", attribution exists via receipts; for filesTouched, no worker attribution — is that OK? The spec ledger lists "files touched" unattributed. Fine.) The toolCalls retention is a spec-plan mismatch: D6 says strip tool noise; plan keeps names. Defensible (names are cheap signal) but it IS a divergence from the decision text. Nit/should-fix.

- Task 4: `ContextAssembly.adaptationPrompt(...implementer: "mellum")` — test expects prompt contains "split into smaller chunks" — the implementation's prompt template includes that phrase literally for ALL implementers ("if \(implementer) is small-capability, split into smaller, more detailed chunks"). So the test passes trivially regardless of implementer size — the prompt doesn't actually vary by capability; it just names the implementer and instructs the model to judge. Is that D7-compliant? D7: "adapt it to the implementer's size/capability (Laguna coarser and more general; Mellum finer...)". The prompt delegates the sizing judgment to the no-think model. Acceptable, but the test is weak (asserts a constant string). Nit.

- Task 4: `assemble` includes `loaded.keys.sorted()` names only — NOT the file contents! "Staged files: a.swift, b.md" — the staged READS (contents) are not in the packet. D5: "prepared context assembled from objective + digest + staged reads". If staged reads' contents aren't included, the worker must re-read them (read aids are free per D8 — "read aids stay free"). Hmm — maybe intent: the packet names staged files; the worker reads them via tools (free). Then `loaded: [String: String]` (path→content) exists only to derive names — why carry contents at all in the pure mapping? And adaptationPrompt embeds... also only names. So contents are never used anywhere. Either assemble should include selected contents (bounded) or the parameter should be `[String]`. Type design smell; and D5's "staged reads" is ambiguous: reads staged INTO the packet vs files staged for the worker to read. Two-way-readable requirement — flag it. Should-fix: pin whether prepared context includes file contents (and a bound), and fix the type accordingly.

- Task 5: `EnvelopeMath.overheadRatio(realizedWin: 4.2) == 1.0` — floating point: 4.2/4.2 == 1.0 exactly? 4.2 is not exact in binary but x/x == 1.0 exactly for any finite nonzero x. Yes, x/x is exactly 1.0 in IEEE754. And 2.1/4.2 == 0.5 exactly? 2.1 and 4.2 are both inexact; 2.1's representation vs 4.2's: 4.2 = 2 × 2.1? In binary, 4.2 = 2.1 * 2 exactly (scaling by 2 is exact). So 2.1/4.2 == 0.5 exactly. OK fine. `realizedWin: 0` → 0/4.2 = 0. Fine. The comment "zeroWinIsNotANumberHazard ... ratio 0, not NaN" — 0/4.2 isn't NaN anyway; NaN would be 0/0. The hazard named is wrong (ceiling is a constant 4.2, never 0) — the test title misleads. Nit.

- Task 5: `PacketPerturbation.taskTextBloat15x` — raw value "taskTextBloat15x" vs spec "×1.5" — naming nit (15x reads as fifteen-x). Nit.

- Task 6: the fixture's `ready` event: `{"t":"ready","stop_reason":"eos",...}` — does AgentEvent have a `ready` case with those fields? Presumably from prior phases. The test's `readPoolEvents` uses `fake.stdout.fileHandleForReading.fileDescriptor` — but `spawnAgent` returned `fake` with `.process` and `.stdout` — is `stdout` a Pipe? `fake.process.terminate()` and `fake.stdout.fileHandleForReading` — OK consistent if FakeAgentProcess exposes stdout: Pipe. Assumed existing harness shape. The bigger issue: `writePrompt(fake, "go")` then read until worker 1 events — the fake replays the whole capture after first prompt? The capture contains orchestrator events AND worker 1 events; does the fake replay all lines on the first prompt, or does it expect one prompt per turn? If the fake is turn-based (replays up to next ready per prompt), worker 1's events (after worker 0's ready at ts:3) would require a SECOND prompt — and the test only writes one. The test would time out. The fixture has two `ready` events (worker 0 at ts:3, worker 1 at ts:5) — a turn-per-prompt fake stops at the first ready. Unless the fake, seeing the prompt addressed to nobody, replays everything. Unknowable from here, but the test as written assumes replay-past-ready behavior. This is exactly where a no-context engineer gets stuck: the test's interaction with the fake's turn semantics is unspecified. Should-fix: state the fake's replay contract (does a bare prompt replay to EOF or to next ready?) and if turn-based, the test must write a second (worker-addressed) prompt — which then requires the inbound worker-prompt encoding to exist in the HARNESS too, tying back to the inbound-contract gap.

Also Task 6 fixture: hello line has `"worker":0` — but Task 8 says worker field only when N>1; the fixture's argv has --subagent-pool 2 so N=2, worker present. Consistent. But PoolWireParserTests feeds hello WITHOUT worker for the N==1 default case — consistent.

- Task 7: `PoolEngine.argv` — "one orchestrator + N-1 worker sessions" comment: `--subagent-pool N` means N total sessions (orchestrator + N-1 workers)? But Task 8: `c.num_workers` — "N sessions" — is N the worker count or total? Task 8 Produces: "`--subagent-pool N` (default 1 = the single-worker behavior...)" — "single-worker" when N=1 suggests N=1 means ONE session (the orchestrator-as-worker). Task 7 comment says N = orchestrator + N-1 workers. Task 6 argv: `--subagent-pool 2` with workers 0 and 1 in the fixture — 2 sessions, one orchestrator + 1 worker. Consistent with N=total. But naming `num_workers` for a total that includes the orchestrator is a confusion trap; and PoolScheduler hands out WorkerIds starting at 1 (nextId=1) with orchestrator=0 — so app-side worker ids 1..N-1 map to engine sessions 1..N-1. OK consistent, but the spec D1 says "hosts N sessions" — N sessions total. Fine. Nit: name it num_sessions or document.

- Task 7 kv test: `data.count > 48` guard throws kvTooShort — a 48-byte file (header only, empty conversation) throws; should an empty conversation be valid? Edge nit.

- Task 7: readKVText is dead code (nothing consumes it) — as discussed, D6 backing undefined. Should-fix.

- Task 9: modifies `Sources/SwiftStarKit/ToolCallbackResponder.swift` — wait, Files says "Modify: `Sources/SwiftStar/ToolCallbackResponder.swift`"? Let me check: "Modify: `Sources/SwiftStarKit/ToolCallbackResponder.swift`" — actually it says `Sources/SwiftStarKit/ToolCallbackResponder.swift`. Hmm, the Files list: "Modify: `Sources/SwiftStar/AgentController.swift`", "Modify: `Sources/SwiftStar/DispatchView.swift`", "Modify: `Sources/SwiftStarKit/ToolCallbackResponder.swift`". So the responder is in Kit (pure) — consistent with "the `dispatch` branch in `respond` (pure)". OK. But then the responder's dispatch branch returns "dispatched as worker N" — needs N — as discussed.

- Task 9 has no explicit failing-test step for the controller loop (only responder tests). The receipt-injection integration test (spec's integration tier) is absent. Blocker/should-fix (I'll fold into one coverage finding or keep separate — separate, since one is "spec integration tier missing", another is "Task 9 mis-sized/underspecified").

- Task 10: "runs one deep-context baseline (131k)" — where does a 131k-ctx orchestrator come from? -c 131072 for the baseline run. And "pooled shallow-context runs (8 × 16k, sequential)" — 8 packets through the pool. But wait — the analytic ceiling compares ONE 131k prefill vs EIGHT 16k prefills. The realized comparison: a deep agent doing the whole task in one 131k session vs an orchestrator+8 workers each ≤16k. But the orchestrator session ALSO prefills (its context grows with receipts). The spec's own caveat: "each shallow session repeats the shared preamble; results fold back into a parent whose context grows and re-prefills" — so realized win includes orchestrator re-prefill costs. The gate must measure the FULL pool path (orchestrator + workers), not just 8 isolated 16k runs, else the ratio is against a strawman. Task 10 Step 1 says "runs one deep-context baseline (131k) and the pooled shallow-context runs (8 × 16k, sequential)" — ambiguous whether the pool side includes the orchestrator's turns. Should-fix: define the measured pipeline explicitly (orchestrator prefill+decode counted).

- Task 10's fallback: "If the real engine cannot be run in this environment, the gate's arithmetic is already pinned by Task 5; record the live run as pending" — this invites shipping P11 with the phase's OWN measurement gate unmeasured. The spec says the phase "ends at the phase's own measurement gate" and "the gate must prove it". A pending gate ≠ proved. The plan's escape hatch contradicts the spec's problem statement. Should-fix: the gate is the phase's exit criterion; if it can't run, the phase isn't done — say that, rather than "record as pending and close" (Task 11 closes the phase regardless). Actually Task 11 Step 3 commits "close — roadmap..." unconditionally. Combined with Task 10's pending-escape, you can close P11 with zero live evidence. Blocker-ish governance hole. I'd say should-fix to blocker. The spec's D11 is the phase's measurement gate; the plan allows skipping it. Blocker.

- Missing from plan entirely: D11's "use-it-or-lose-it governance" — no task records the flag-gate-or-revert commitment (ROADMAP note in Task 11?). Nit.

- Missing: snapshot/rewind wiring for the gate — spec AppKit components: "snapshot/rewind wiring for the gate". No plan task implements snapshot/rewind wiring (Task 10 measures snapshotSaveCount/RestoreCount — from what? If nothing snapshots, counts are 0 — which D11 says "zero in v1 is itself a finding", so maybe fine — but then why does EnvelopeReport carry the counters and the spec name "snapshot/rewind wiring for the gate" as an AppKit component? If v1 has no snapshot/rewind, the spec component list over-promises. Contradiction inside the spec: D11 says eviction/snapshot instrumentation answers "does the pool need idle-session eviction at all (zero in v1 is itself a finding)" — implying v1 does NOT do snapshot/rewind of idle tiers; but Components says AppKit includes "snapshot/rewind wiring for the gate". Two-way readable / contradictory. Should-fix: cut the component or scope it to "counters only".

- Spec Testing fast tier: "the envelope computation" — Task 5 ✓. "receipt correlation" — Task 1's DispatchReceipt + Task 2? "Receipt correlation (worker N's outcome lands on orchestrator turn N+1)" is listed under INTEGRATION in the spec. Fast tier lists "receipt correlation" too — both tiers list it. Plan has no Kit "receipt correlation" type (maybe PoolScheduler.pendingDelivery is it). Eh — the spec lists receipt correlation in both tiers; the plan covers neither explicitly beyond pendingDelivery. Fold into the integration-gap finding.

- HandoffPacket init in Task 2 test: `HandoffPacket(taskText:writableFiles:validationCommand:baselines:turnBudget:toolCallBudget:)` — assumes P10's exact signature; fine as settled.

- Task 2: `nextWorker` returns smallest id — pending is a Dictionary; min(by:) on dictionary — fine.

- PoolScheduler: no failure path! PoolCommand has no `.workerFailed` — the spec's Kit components list says the transition function includes "fail": "enqueue, run, complete, fail, receipt-inject". The plan's PoolCommand lacks a fail case; workerFinished with a refusal receipt covers budget-exceeded, but what about engine crash / tool errors / timeouts? D11's failure injection assumes failures happen and a retry policy exists. The scheduler can't represent failure. Spec coverage gap — should-fix (add .workerFailed or define failure-as-receipt mapping). The spec literally names "fail" as a transition; the plan dropped it.

- Also PoolState has no notion of worker→worktree or worker→session resources; fine for pure.

- Task 8 Step 4: "the worker struct fields (session, transcript, status, turn-outcome snapshot) all become per-array-element" — transcript per worker: memory for N transcripts in the engine; fine.

- Task 8: hello caps — should the pool advertise a cap (e.g., "pool")? The fake's hello caps are ["text","tool","status","ts"]; if the real engine adds a "pool" cap when N>1, the hand-authored fixture (no pool cap) diverges from the real capture → Task 6 test asserts... the test doesn't check caps. But the recapture replaces the fixture anyway. Minor. But worth noting: contract versioning — how does the app KNOW the engine is pool-capable? caps! If no cap advertises it, the app can only try --subagent-pool and fail. The spec/plan never define capability negotiation for the pool. Should-fix/nit: add a cap or version bump so the app can detect support (matters for the use-it-or-lose-it flag-gating too — gating off requires detection).

- DispatchView updates (Task 9): "surface the pool" — vague; no test; SwiftUI layer untestable in tiers. Nit (accept UI vagueness) but flag that Task 9 mixes three layers (Kit responder, App controller, SwiftUI) — mis-sized; split responder (pure, test-first) from controller wiring (integration-test the enqueue→run→inject loop against the fake) from view.

- Global constraints: "Binding rule 6 (evidence floor: the fake is regenerated from a committed capture; a worker-tagged capture parses and routes)" — fine.

- Standing rule: "every submodule bump owes a golden-fixture recapture (the C patch in Task 8 is a bump)" ✓ Task 8 Step 6.

- Task 11: runs engine suite; updates ROADMAP concept budget with two terms ✓ matches spec concept budget.

- Spec concept budget: "Two new terms" — fine.

- Spec Out of scope: "merging any candidate ref (the parent reviews; merge is a human act)" — "the parent reviews" — the parent MODEL reviews? Fine.

- D3: "No new event kind, no second fork round for emission." — "no second fork round" — jargon; fine.

- D4: "No mid-turn session suspend/save/load in v1" — good, rules out save_payload mid-turn.

- One more spec-internal tension: Problem statement says "context-isolated subagents sharing one locked engine (one model load, N sessions in one process), driven through a queue over the serialized GPU, ending at the phase's own measurement gate. The win being bought is the context curve, not concurrency — and the gate must prove it." vs D11 gate allowing pending. Covered above.

- The 4.2x math: 2121/500 = 4.242 — ✓ consistent.

- Memory formula: min(ctx,16384)×375156 — at ctx=16384 → 6.146GB ✓ "~6.1 GB" ✓. KV at ctx=16384: 49152×16384 = 805,306,368 + 75,497,472 ≈ 880MB ✓ (spec doesn't state, fine).

- Spec says "a 4k worker is ~1.5 GB" — 4096×375156 = 1.536GB ✓.

- Now, D8 "worker's ctx_size equals its budget" + HandoffPacket.turnBudget — units? turnBudget 1000 in tests — if tokens and ctx_size=1000... but scratch formula min(ctx,16384)×375156 suggests scratch scales with ctx below 16k. ctx=1000 → 375MB. Plausible. But is turnBudget tokens or turns? P10 settled — can't re-litigate, but the plan never wires turnBudget→ctx_size. Already flagged.

- PoolWireParser: parses the line TWICE (JSONSerialization + AgentWireParser) — perf nit, fine for tests; production drain could be hot at high event rates. Nit.

- PoolWireParser.extractWorker: `object["worker"] as? NSNumber` — a JSON `true` bridges to NSNumber too (CFBoolean)! `"worker":true` would pass and intValue==1 → worker 1. Malformed test uses "nope" (String) → default 0 ✓, but bool hazard exists. Also fractional worker 3.7 → intValue 3. Nit-level robustness; the C side emits ints. Nit.

- WorkerId Codable: encodes as {rawValue:...}? With RawRepresentable+Codable synthesized — actually for a struct with let rawValue and memberwise Codable synthesis, JSON is {"rawValue":7} — the test round-trips through the same synthesis so it passes; fine. But if DispatchReceipt persists to a ledger file, the shape is stable? Fine.

- DispatchReceipt.injectionPrompt: "Worker 2 refused: budgetExceeded" — test expects contains("refused") ✓ and reason ✓.

- Task 6: `FakeAgentHarness.fixture("pool.ndjson")` — assumes a fixture helper exists (P10 presumably). OK.

- Task 6 argv includes "--shell","off" and "--workspace","/tmp/w" — matching existing fake validation presumably.

- Integration test target import: `@testable import SwiftStarAppKit` plus `import SwiftStarKit` — but PoolEngineTests also uses FakeAgentHarness (in integration tests target) ✓.

- Task 7's test placement: kv test inside PoolEngineTests (integration tier) — reading a temp file; fine, though it's pure-ish file IO; could be fast tier if Kit did IO — no, Kit is no-IO, so AppKit integration ✓.

- Task 8 C tests named test_agent_* — consistent with existing suite ✓.

- Task 8 Step 2: need_arg signature — assumes existing helper ✓ (house style).

- Task 8: "Record line numbers in the commit message" ✓ good practice.

- Risk note: split (a)/(b) — good. But (a) "flag + worker field emission for N==1 (additive, zero-behavior-change)" — wait: worker field emission only when N>1; so (a) with N==1 emits nothing — truly additive ✓. But then (a) alone has no observable behavior — its C tests force num_workers>1 internally? test_agent_emit_event_carries_worker_id would construct a config with N>1 — fine, unit-level.

- What about the fork ledger update — Task 8 files list it ✓.

- Missing acceptance for "one generating at a time" — flagged.

- Missing: engine-side session creation timing & sysprompt.kv sharing: D1 "The shared sysprompt.kv prefix serves every worker read-only" — Task 8 doesn't mention loading sysprompt.kv per session (ds4_session_common_prefix / load path). The session primitives exist; presumably session_new handles the bootstrap prefix. But the plan should state that each pooled session loads the shared prefix read-only — if ds4_session_new by default does this, fine; unstated assumption. Should-fix/nit: Task 8 should name the prefix-sharing call per session (and a C test that two sessions share the prefix without copying — memory!). Actually memory: does each session's KV INCLUDE the sysprompt tokens? KV = 49152×ctx+75MB per session — if the bootstrap prefix is re-materialized per session, that's the "each shallow session repeats the shared preamble" cost — consistent with spec. OK.

- The gate's "canonical packets" — where do canonical packets come from? A curated set? Unspecified repository location/content. Task 10 says "builds the packet set" — from what corpus? A no-context engineer can't invent the canonical packet set — it defines the gate's headline number. Should-fix: name the packet corpus (e.g., N packets from P10's validation set, committed under fixtures/gate/).

- EnvelopeMath.report: overheadRatio from .canonical only ✓ matches D11(a).

- Perturbation wins need the deep baseline re-run per perturbation? winByPerturbation maps each perturbation to a win — each requires its own deep baseline (bloat ×2 changes the deep run too). Task 10 doesn't say; cost: 5 arms × (1 deep + 8 shallow) runs at minutes each — the gate is expensive; "minutes" claim in Task 10 ("just capture-style, minutes") is optimistic: one 131k prefill is ~2,121s ≈ 35 minutes ALONE. The spec's own number! One deep baseline = ~35min; the gate with 5 arms × deep runs = ~3 hours. "minutes, never CI" undersells by an order of magnitude. Should-fix: size the gate honestly (cache/share the deep baseline across arms where the perturbation doesn't affect the deep side — bloat DOES affect deep; failure injection maybe not). Good catch — the plan's own time estimate contradicts the spec's measured 2,121s figure.

- Also failure injection "at fixed rates under a fixed retry policy" — retry policy undefined anywhere (P10? DispatchOutcome had no retry). And PoolScheduler lacks retry/fail transitions (flagged). Link these.

- D11(b) "the threshold where the win stops justifying the fork" — needs a cost model for "justifying" (win > 1? > overhead of fork maintenance?). Unactionable as stated; it's a human judgment on the envelope — fine if labeled as such. Nit.

- Spec Data flow step 2: "the P9 responder revision-checks mutations against packet.writableFiles" — the responder is host-side; with workers in worktrees, the responder must know WHICH packet (per worker). Task 9 doesn't wire packet→responder context per in-flight worker. The responder is a pure function of (request, consent/params) — now it needs the active worker's packet. Interface change to ToolCallbackResponder not designed. Should-fix (fold into Task 9 finding).

- Security/consent: dispatch spawns workers that mutate worktrees — does dispatch require user consent (P9 consent checks)? Task 9 Step 1 mentions "The `consent` check refuses dispatch unless the params carry a non-empty taskText..." — that's a well-formedness check, not consent. Whether dispatch needs user consent (it triggers file mutations eventually) — spec silent. Nit/should-fix? The worker mutations go through the normal per-tool consent inside the worker turn presumably. OK nit.

- One more: spec says pool ledger reconstructed "never trusted to the model's summary" (D9) ✓ plan's digest is host-side ✓.

- Spec: "SwiftStarKit (pure — no Process, no IO): ... the packet-maker's pure mapping" ✓ Task 4. "the envelope computation" ✓ Task 5. "receipt correlation" — hmm no dedicated type; pendingDelivery + injection. Weak coverage; fold in.

- Plan File Structure lists `DispatchView.swift` updates ✓ Task 9.

- Plan "Tech Stack: ... Fake engine generated from